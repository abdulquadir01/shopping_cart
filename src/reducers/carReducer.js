

const cartReducer=(state,action)=>{
    switch (action.payload) {
        case "ADD_TO_CART":
            return 
    
        default:
            return state;
    }
}

export default cartReducer;
